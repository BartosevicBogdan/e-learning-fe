import LecturesCluster from './LecturesCluster';

export default {
  title: 'Atomic Design/Organisms/LecturesCluster',
  component: LecturesCluster,
};

const Template = (args) => <LecturesCluster {...args} />;

export const Example = Template.bind({});
Example.args = {};
